{
  "name": "interviewready",
  "version": "1.0.0",
  "description": "A production-grade multi-agent system for structured evaluation of human inputs (resumes, interview responses, and job alignment tasks), designed to simulate real-world decision workflows with governance, traceability, and observability.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "doctor": "node scripts/doctor.mjs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:frontend": "cd frontend && npx vp check --fix",
    "lint:backend": "cd backend && uv run --no-sync ruff check --fix",
    "lint:backend:imports": "cd backend && uv run --no-sync lint-imports",
    "lint:all": "npm run lint:frontend && npm run lint:backend && npm run lint:backend:imports",
    "prepare": "husky"
  },
  "packageManager": "npm@11.16.0",
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
