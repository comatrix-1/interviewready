You are Claude Code acting as an automated developer agent working on GitHub issue #3: "Refactor high-complexity agent functions (reduce cyclomatic complexity)" in this repository (working directory is the repo root).

Goal
- Reduce cyclomatic complexity for the highest-complexity functions in backend/app/agents to be noticeably lower (aim for complexity <= 15 where reasonable) while preserving existing behavior and tests.

Scope & constraints
- Primary targets (from codebase analysis):
  - backend/app/agents/interview_coach.py  (function: process — complexity ~26)
  - backend/app/agents/job_alignment.py   (function: _parse_json — complexity ~25)
  - backend/app/agents/content_strength.py (inspect for hotspots)
- Only modify files under backend/app/agents and their related tests. Do NOT change frontend files or unrelated modules.
- Prefer small, safe refactorings: extract helper functions, reduce nesting, early returns, small pure-function extraction. Avoid large architectural rewrites.
- Add or update unit tests as needed to cover behavior; do not remove existing tests.

Workflow requirements
1. Analyze the target functions and explain the root causes of high complexity.
2. Propose minimal refactor steps (list of changes). Get automatic approval (no human in loop) and implement them incrementally.
3. Create a working branch named claude/issue-3-YYYYMMDD (use today's date). Commit each logical change with a clear message.
4. After each commit, run tests (pytest -q) and include test results. Run lint if available (ruff, flake8, or pylint) and include results.
5. At the end, run the full test suite, push the branch to origin, and open a PR titled "Refactor: reduce complexity in agent functions (issue #3)" with a description summarizing the changes, motivation, tests run, and link to issue #3. Use GitHub CLI for PR creation (gh pr create --fill).

Outputs required (JSON)
Return a single JSON object with these fields (valid JSON only):
{
  "summary": "short summary text",
  "root_causes": ["file:function - cause summary"],
  "plan": ["step descriptions"],
  "changes": ["list of modified file paths"],
  "patch_unified": "git diff -U3 output (unified diff)",
  "commands_run": [{"cmd":"...","exit_code":0,"stdout":"...","stderr":"..."}],
  "tests": {"ran":["pytest -q ..."], "passed": true|false, "stdout":"..."},
  "pr_url": "https://github.com/.../pull/NNN"  // if PR created
}

Important behavioral constraints
- Preserve original behavior: do not remove existing functionality. If a change may alter public API, add tests that assert previous behavior.
- Keep changes small and well-documented in commit messages.
- If you encounter uncertainty requiring human judgment (e.g., ambiguous behavior), implement conservative change and note the uncertainty in the summary.

Permissions
- Allowed tools: Read, Edit, Bash, Git. You may create branches, commit, push, and open PRs (gh CLI is available).

Run commands you need. Provide the final JSON output as stdout. Include full unified patch and command outputs in the JSON fields. Do not modify files outside the allowed scope.

If anything blocks progress (missing dev dependencies, failing tests unrelated to these files, or permission errors pushing), report the error in the JSON under tests.stdout or commands_run entries and stop further changes.

Begin.